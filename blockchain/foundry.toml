[profile.default]
src     = "contracts"
out     = "out"
libs    = ["lib"]
test    = "test"
script  = "script"
solc    = "0.8.24"

# Monad Testnet
[rpc_endpoints]
monad_testnet = "${RPC_URL}"

[etherscan]
monad_testnet = { key = "${ETHERSCAN_API_KEY}", url = "${EXPLORER_URL}" }
